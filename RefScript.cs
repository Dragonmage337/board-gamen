using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RefScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

//This is a comment. The computer ignores this but keeps it in the code anyway.

    // Update is called once per frame
    //This is a function
    void Update()
    {
        //This is a variable
       int i = 0;
       //This is a loop
       while (i < 5)
       {
           //This is a statement
           Debug.Log("Printed");
       }
    }
}
